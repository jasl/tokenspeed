-r common.txt
nvidia-cutlass-dsl[cu13]==4.6.0
nvidia-cutlass-dsl-libs-cu13==4.6.0
torch==2.11.0
flashinfer-python==0.6.14
flashinfer-cubin==0.6.14
nvidia-ml-py==13.580.82
nvtx
# Multi-node host-staged RoCE (DGX Spark GB10 class): 2.30.4 is the ONLY NCCL
# safe for CUDA-graph-replayed collectives on BOTH torch 2.11 and 2.13 capture
# paths (2.28.9/2.29.7/2.30.7 all hit the proxy-progress-death class; minimal
# repro: ds4-sm120-harness run_gb10_nccl_graph_replay_gate.sh). torch's own
# ==2.28.9/2.29.7 dep re-resolves over any manual pin on every editable
# install, so the pin lives here to survive rebuilds.
nvidia-nccl-cu13==2.30.4; platform_system == "Linux"
